// No longer needed as an EnvironmentKey for @Environment(\.viewing), but if other code uses it, keep as is.
// If you want to support both ways (env and binding), add documentation to clarify that mutation is only via binding or parent state now.
